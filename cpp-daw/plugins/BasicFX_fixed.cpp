#include "BasicFX.hpp"
#include <cmath>
#include <algorithm>

namespace DAW {

// Empty implementation - the class definitions in the header are complete inline implementations
// This file is just needed for compilation but the actual implementations are in the header

} // namespace DAW
