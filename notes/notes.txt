I created a exciting new audio synthesis tool called CraftySound. CraftySound lets users craft their own synth preset. CraftySound allows users to add as many oscillators as they want to create a super unique polyphonic sound, which you can export as a Json file and import back into CraftySound for further editing. Using the built in synthesizers functions of Tone.js, each oscillator can have largely different audio properties:
-Instrument type (Selects the base tone.js function instrument, )
-Waveform (17+ types of waves you can choose from)
+Choosing different waveforms unlocks a personalized set of controls for that waveform
+Detune and Volume controls (self-explantory)
+Phase dial (delays wave in microscopic amounts of time, which causes increases and decreases in amplitude in certain harmonics with other waves) 
+Envelope, filters and resonance controls
-3 live visualizers: waveform, spectogram and harmonic analyzer
-Virtual Midi keyboard to trigger notes
-CUSTOM DRAW WAVEFORM (this is my favorite feature, although it doesn't behave consistantly)
-Tone.js audio FXs, imported the code from ToneJSDeepExploration tool
+Each FX has its own custom set of parameters

*Context: I've been wanting to create an audio tool to allow user to customize a sound preset at a low level.
I created other tools like ParameterAudioEditor and Wavetable_Synth to achieve this, but they didn't work like 
I wanted it to. It was only till I learned to incorporate the audio synthesis capabilities of Tone.js, Meyda.js and Essentia.js, that allowed me to add the feature of custom drawing a waveform.

Video demonstrates me going through most
 of the features of CraftySound, exporting the json and importing it back.

Create an txt file making a plan to set up this codespace for C++ wasm to make potential C++ web applications
with super advanced, powerful, low level editing, analysis, synthesis of audio and music.
Make sure to not create the application(s) yet, as I haven't decided what exactly I want the apps to do.
I need to know what the C++ wasm libraries are capable of.

Also note that I don't want to install unnecessary stuff, like js libraries that can totally be called through cdn or unpkg,
unnecessary C++ tools. I only have 1 gb of storage for this codespace.

make an voice autotune FXs filthy understanding

continue the installation of libraries and tools, make sure to use the best known functional method to install each library. 
ground with google search.

create one c++ wasm application using juce and install C++ wasm modules that 
Spectral Analysis:
Fast Fourier Transform (FFT): The juce::dsp::FFT class allows for efficient computation of forward and inverse FFTs, which is fundamental for converting time-domain audio signals into the frequency domain for detailed spectral examination [1].
Magnitude and Phase Analysis: Once the FFT is computed, the magnitude and phase information of individual frequency bins can be extracted and analyzed in real-time [1].
Filtering:
IIR and FIR Filters: The framework includes classes for implementing various Infinite Impulse Response (IIR) and Finite Impulse Response (FIR) filters, such as Butterworth, Chebyshev, and Bessel filters, allowing for precise frequency band isolation or removal of noise for subsequent analysis [1].
State Variable Filters: juce::dsp::StateVariableFilter provides versatile filter types (low-pass, high-pass, band-pass, notch, etc.) with resonance control for detailed spectral shaping and analysis [1].
Time-Domain Analysis:
Envelope Following: Tools for tracking the amplitude envelope of a signal, useful for dynamic range analysis and feature extraction [1].
Peak and RMS Level Detection: Specific classes help measure peak and Root Mean Square (RMS) levels in real-time, critical for loudness analysis and dynamics processing [1].
Advanced DSP Functionality:
Convolution: The juce::dsp::Convolution class can be used for impulse response analysis and the application of frequency-domain effects [1].
Oversampling: The juce::dsp::Oversampling class allows for increasing the sample rate to perform analysis with reduced aliasing artifacts, leading to more accurate results, especially at higher frequencies [1].
Pervasive DSP utilities: The module includes various other utilities and building blocks that can be combined to create more complex analysis tools, such as custom pitch detection algorithms or sophisticated feature extraction methods [1]. 

Now instead of audio analysis, make it low level polyphonic audio editing studio. Use JUCE framework to make application look like a projessional plugin. Edit and add the new features in C++ Audio Studio.

install iPlug2 and use it for the C++ app

I mean the outside UI, not just the graphs (the surrounding UI still looks like html, with the google font and stuff).
also make the visualizations live, so user can play audio file and see the graphs change.

Update accurate links in index.html containing "planets" to my audio tools.

for editor.html in cpp audio studio, add smooth mouse interactive low level editing features, that are super low level, like dragging the harmonics points, draggin waveform peaks of different graphs, like amplitude, frequency.
This requires adding detailed audio analysis features. using cpp for backend

In index.html with all the planets: make planets midi daw with ideas and CraftySound at least twice as big as normal planets.
Make Midi daw noticebly bigger than CraftySound, and make Audioverse Sun bigger than midi daw.
when user clicks highlight planet; draw a red rectangle around it.
Also the controls info box at bottom right corner and the list of planets overlap. Move list up to top right corner to make sure they don't overlap.
Changes colors of welcome screen and dialogue box that appears if you click on item in list to darker background colors to accommedate the text colors.

The highlighting planet feature doesn't show any rectangle or anything. It just shifts the zooming focus to the planet highlighted. Fix this.
The planets appearance looks bad (spheres with random 3d shapes inside). Be proactive with coming up with the new unique design for each individual planet.
Make the orbit patterns less repetitive (right now all orbit paths are circles around the sun), making some of my no so important tools, like moons orbiting a planet, oval orbit, random distance for each full orbit.
Keep in mind throughout the whole thing, that I want a music/universe theme.
Also have an animated 3d red arrow pointing at "controls" info box at bottom right corner, until user does first action after clicking "start exploring".

For AudioVerse, create a tutorial similar to Midi DAW with Ideas "Riff" character tutorial. Point out placeholders and pretty much all features, like an interactive game tutorial. In tutorial, have user click to switch views.j

Don't require user to do any action for tutorial, except for switching views, so the red rectangles and arrows can point out the controls on every view

whenever tutorial moves, the outside darkens and user can't switch views.

don't darken anything for tutorial, it makes it impossible to switch views

the waiting of tutorial waits forever, also make riff button glow, after tutorial is skipped or done, make button is gone.
make metronome strip controls (has metronome, tempo, settings button) be on same strip as transport controls and delete metronome strip. because transport strip is blocking metronome strip.

on second thought, just make sure the 2 strips don't 

that didn't solve it, the problem is that I expanded the transport strip vertically, so it now covers the metronome strip. so move the metronome stip down out of overlap

@ create a open pull request in a branch called secondary